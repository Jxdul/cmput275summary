{
  "name": "cmput275summary",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node tools/build-site.mjs",
    "predeploy": "mkdir -p public/examquestions && cp examquestions/*.txt public/examquestions/ 2>/dev/null || true",
    "deploy": "wrangler deploy"
  }
}
